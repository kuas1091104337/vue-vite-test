import { defineStore } from 'pinia';
export const useVuexApiStore = defineStore('VuexApi',() => {
  const photoArr = ref([]),
        idx = ref(0),
        isLoad = ref(false);
  return { refName }
})